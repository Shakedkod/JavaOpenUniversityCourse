public class PlaceComplexity
{
    // place complexity: O(1) - the function does not define the array
    public static void printArray(int[] a)
    {
        for (int i = 0; i < a.length; i++) {
            System.out.println(a[i]);
        }
    }
}
